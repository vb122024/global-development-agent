# Evaluations

Run the deterministic evaluation checks from the repository root:

```bash
backend/.venv/bin/python evals/run_checks.py
```

The checks cover factual values, citations, limitations, prompt-injection
handling and unsupported causal language.
